# Copy to `.env` and adjust as needed. NEVER commit the real `.env`.
# This rules-based service needs no secrets; PORT is the only knob.

# Port the HTTP server binds to. Most PaaS platforms inject this automatically.
PORT=8000
