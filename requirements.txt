# Runtime dependencies only (kept minimal for fast, reliable container builds).
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
# Optional: only used when REDIS_URL is set (distributed rate limiting).
redis==5.2.1
